<!DOCTYPE html>

<title>Contrat</title>

{% block body %}
    <h1>Contrat</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ contrat.id }}</td>
            </tr>
            <tr>
                <th>Date_expiration</th>
                <td>{{ contrat.dateExpiration ? contrat.dateExpiration|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Max_cont</th>
                <td>{{ contrat.maxCont }}</td>
            </tr>
            <tr>
                <th>Max_vrac</th>
                <td>{{ contrat.maxVrac }}</td>
            </tr>
            <tr>
                <th>Max_roulant</th>
                <td>{{ contrat.maxRoulant }}</td>
            </tr>
            <tr>
                <th>Id_utilisateur</th>
                <td>{{ contrat.idUtilisateur }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('contrat_index') }}">back to list</a>

    <a href="{{ path('contrat_edit', {'id': contrat.id}) }}">edit</a>

    {{ include('contrat/_delete_form.html.twig') }}
{% endblock %}
