<form method="post" action="{{ path('bureaucitoyen_delete', {'id': bureaucitoyen.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ bureaucitoyen.id) }}">
    <button class="btn btn-block btn-danger">Supprimer</button>
</form>
