<!DOCTYPE html>

<title>New Service</title>

{% block body %}
    <h1>Create new Service</h1>

    {{ include('back/service/_form.html.twig') }}

    <a href="{{ path('service_index') }}">back to list</a>
{% endblock %}
