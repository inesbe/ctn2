<form method="post" action="{{ path('medias_delete', {'id': media.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ media.id) }}">
    <button  class="btn btn-default"><i class="far fa-trash-alt"></i> Supprimer
    </button>
</form>
