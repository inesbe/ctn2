<form method="post" action="{{ path('info_pratique_delete', {'id': info.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ info.id) }}">
    <button class="btn btn-block btn-danger btn-xs">Delete</button>
</form>
