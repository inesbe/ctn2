<!DOCTYPE html>

<title>ConditionUtilisation</title>

{% block body %}
    <h1>ConditionUtilisation</h1>

    <table class="table">
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ condition_utilisation.id }}</td>
        </tr>
        <tr>
            <th>Fichier</th>
            <td>{{ condition_utilisation.fichier }}</td>
        </tr>
        </tbody>
    </table>

    <a href="{{ path('condition_utilisation_index') }}">back to list</a>

    <a href="{{ path('condition_utilisation_edit', {'id': condition_utilisation.id}) }}">edit</a>

    {{ include('condition_utilisation/_delete_form.html.twig') }}
{% endblock %}