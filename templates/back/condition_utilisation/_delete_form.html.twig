<form method="post" action="{{ path('condition_utilisation_delete', {'id': c.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ c.id) }}">
    <button class="btn btn-block btn-danger btn-xs">Delete</button>
</form>