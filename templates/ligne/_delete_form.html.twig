<form method="post" action="{{ path('ligne_delete', {'id': l.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ l.id) }}">
    <button type="submit" class="btn btn-block btn-danger btn-xs">Delete</button>
</form>
